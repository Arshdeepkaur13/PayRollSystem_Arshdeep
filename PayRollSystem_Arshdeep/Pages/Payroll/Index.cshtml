@page
@model PayRollSystem_Arshdeep.Pages.Payroll.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Payroll[0].NoOfLeaves)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Payroll[0].GrossSalary)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Payroll[0].TotalDeduction)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Payroll[0].NetPay)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Payroll[0].Employee)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Payroll) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NoOfLeaves)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GrossSalary)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalDeduction)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NetPay)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Employee.ID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
